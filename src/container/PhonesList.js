import React from 'react';

const PhonesList = props => {
  // eslint-disable-next-line
  const { phones } = props;

  return (
    <div>
      <h1>HOT DEALS</h1>
    </div>
  );
};

export default PhonesList;
